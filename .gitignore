# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
# Environment files (any directory, any variant) — never commit secrets.
# `.env` matches the basename in every subdirectory (incl. server/, hermes/).
.env
.env.*
server/.env
hermes/.env
!.env.example
!server/.env.example
!hermes/.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Database files
*.db
*.db-wal
*.db-shm
*.db.bak
server/data/
server/uploads/
uploads/

# Generated report PDFs (hermes fixtures excluded from history)
hermes/dist/

# Temp files
tmp/
*.tmp
*.tFcgf7rS.*

# Editor artifact junk (mis-parsed CLI args creating literal files)
--clip*

# AI/IDE tool directories (personal, not for public)
.mimocode/
.playwright-mcp/
.kiro/

# Stale directories
path/
/.opencode

server/e2e-data.db*

test-results/
playwright-report/
