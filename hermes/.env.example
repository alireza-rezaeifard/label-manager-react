HERMES_PORT=3002
WORKSPACE_PATH=/workspace
NODE_ENV=production
LOG_LEVEL=info
# Browser origins allowed to call Hermes directly (comma-separated).
# Browsers never need this in production (all traffic goes via /api/ai);
# leave unset to allow only local dev frontends.
# HERMES_ALLOWED_ORIGINS=http://localhost:3000
